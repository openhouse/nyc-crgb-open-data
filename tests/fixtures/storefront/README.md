These CSVs are **synthetic** fixtures shaped to mimic the NYC Open Data LL157
storefront aggregate tables. They are kept for offline development or tests and
should never be used for published indicators. The default pipeline ignores
these files unless you explicitly disable open data by setting
`CRGB_USE_OPEN_DATA=false`.
